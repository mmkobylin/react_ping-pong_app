import { connect } from "react-redux";
// import Player from "./Player"

const mapStateToProps = {( Difference )} => {
        return {
        Difference = state.Difference
    };
};
        
export default connect(mapStatetoProps)(Difference);